git notes
git init creates hidden .git directory
git add filename adds file to staging area
git commit -m message describing changes
git status gives status of staging area
git log gives history of commits

git labels
  HEAD points to most recent commit on current branch
  master points to master branch (also most recent commit)

git branch branchname creates new branch
  git branch without a name tells you which branch you are on
  new branch starts from current status of the active branch
git push -u origin master
  -u only needs to be run once, specifies where to push to/from
  after the first time, just use git push

  to get commands:
    man git push

